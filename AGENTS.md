# Cat's Tower repository instructions

更新日: **2026-08-27**  
Repository: `2hg7trp7rv-design/cats_tower`  
書込みbranch: **既存の`kimi`のみ**  
Current: **Step 1 Round 008 final preseal review — IN_PROGRESS**  
Step 2〜6: **BLOCKED**

## Branch hard lock

- 書込みは既存`kimi`のみ。毎回live HEAD/treeを再取得する。
- branch作成・切替・書込み・削除、PR、merge、rebase、cherry-pick、force-pushは禁止。
- Production alias、課金商品公開、広告network有効化、data deletionは明示承認なしに実行しない。

## Authority order

1. 最新のユーザー明示決定
2. `CHATGPT_PROJECT_INSTRUCTIONS1.md`
3. active change-control、最新addendum、user-decision-lock
4. liveで検証されたStep 1 seal
5. seal対象`MASTER_SPEC.md`、`PROJECT_STATUS.json`、`canonical/*`
6. Acceptance、critics、judge、handover、deployment evidence
7. lower canon、Step 2 executable contract、runtime
8. historical PASS、legacy、参考資料

過去証拠は改変しないが、現行許可に使わない。

## Preflight

1. repository/branch/HEAD/tree確認
2. Project source→active control→seal/status→対象contractの順に読む
3. 許可されたstep/write boundary確認
4. Acceptanceを変更前に固定
5. exact changed paths、failure conditions、evidence planを固定

矛盾があれば後工程を停止し、同じ変更管理で直す。

## Product non-negotiables

- cat/catfolk、常設4体、一時増援別層
- shop/income/delivery/recruitment/reinvestmentはcombat support
- unbounded visible tower、100F milestone、101F+
- tap direct damage 0、auto/offline基礎、mandatory collection/refill tappingなし
- one reset `reset.tower_return`、Floor 1、高速reclear、repeat-best ruby 0
- uncapped coin level、every-100 ruby evolution、evolution非gate
- rarity `N < R < RR < SR < SSR < UR`、N/R deterministic/long-term utility
- separate character/weapon gacha、100/200 targets、carryover/exchange/history
- first copy functional、20+ optional full mastery、diminishing returns、overflow
- login/payment/rewarded opt-in ads、forced interstitial/banner/PvP等は初期除外
- S01〜S12、server-authoritative permanent economy
- arbitrary-precision numeric contract

## Current Step 1 artifacts

`MASTER_SPEC.md`、`FLOORS_1_10_DESIGN.md`、7つの`canonical/*`/contractはfinal preseal review対象。contentが揃っていてもvalid sealなしにStep 2を許可しない。

## Legacy simulation and workflow

V1 candidate/schema/validator、旧workflow、legacy runtimeはhistorical/implementation gap。現行promotion実行、in-place延命、old observed holdout再利用を禁止する。Step 2は新V2 artifactを作る。

## Completion

- 10 separated criticsとfinal judge
- unresolved P0/P1=0
- current-authority contradiction/unclassified path/match=0
- exact frozen content tree、critic/judge evidence、matching `kimi` Preview、seal、activation evidence
- forbidden path/Production change=0

Vercel `READY`だけでPASSにしない。physical iPhone証拠なしに実機確認済みとしない。
