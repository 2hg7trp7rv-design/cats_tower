# Cat's Tower — Claude Code Start Prompt 1

The executable start prompt is maintained at:

`docs/v2/CATS_TOWER_CLAUDE_START_PROMPT1.md`

Read that file from the checked-out repository. This root file is only a stable pointer so that handoff instructions cannot fail because of an assumed path.
