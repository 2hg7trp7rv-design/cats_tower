# Cat's Tower — 戦闘プロトタイプ仕様 (kimi ブランチ)

> **文書状態: LEGACY_SUPERSEDED（2026-08-25）**
> これは旧プロトタイプの履歴資料であり、現行Step 5の実装契約ではない。`MASTER_SPEC.md`、`FLOORS_1_10_DESIGN.md`、`PROJECT_STATUS.json`、`simulation/candidate-v1.json`により置換済みで、本文の44pt、1.6〜2.2秒、仮音許可、対象外機能、ファイル上書き指示を新規実装へ使用してはならない。

本仕様は `MASTER_SPEC.md` (100F正本) と `FLOORS_1_10_DESIGN.md` (1〜10F完全設計) を上位仕様とし、
**戦闘プロトタイプの実装契約と合格条件だけ**を固定する。上位仕様と矛盾した場合は上位仕様が正しい。

## 0. 目的とスコープ

旧 v0.8.2 の最大欠陥（戦闘の可読性・UI占有率・イベントの文章化・難易度曲線）を、新仕様準拠の
**1〜10F 縦切りプロトタイプ**で検証する。目的は「技術的に動く」ではなく
「初見プレイヤーが5秒で『猫が塔を戦って登るゲーム』と理解できる」こと。

- IN SCOPE: 戦闘画面1枚の商用品質化、1F〜10Fの一続きの進行、猫4匹・増援3役・敵6種+エリート2+ボス、店4種、支援2種、5F救出、8F壁、9F遺物3択、10Fボス3形態、階制圧→登階→店舗化の視覚ループ
- OUT OF SCOPE: セーブ schema3、夜明け、100F塔閲覧モード、収益化、BGM/SE素材（WebAudio仮音でよいが±50ms同期は守る）、多言語対応

## 1. 技術構成

- バニラ HTML/CSS/JS。ビルドステップなし。React等のフレームワークは使わない（既存資産・ホスティング形態との整合のため）
- ルート配置: `index.html` / `styles.css` / `game-data.js` / `game-core.js` / `app.js` / `manifest.webmanifest` / `sw.js`（旧 runtime は git 履歴と `.github/baselines/` に保全済み。上書きしてよい）
- スマートフォン縦画面専用（基準幅 390px）。主要操作は親指が届く下部、タップ目標 44×44pt 以上
- `touch-action: pan-y`、タップと縦スクロールの誤判定ゼロを目指す

## 2. 画像アセット契約 (assets/prototype/)

全キャラ素材は透過PNG・1:1・右向き側面・ピクセルアート調で、メタデータを `game-data.js` に定義する:
`footAnchor / headAnchor / contactAnchor / displayScale`（MASTER_SPEC §7.1）。足裏と床のずれ 2px 以内。

| パス | 内容 |
|---|---|
| `assets/prototype/cats/{mugi,luna,toto,kohaku}.png` | 名前付き猫4匹。姿勢・シルエット・走り方で役割判別できること（ムギ=前衛・がっしり、ルナ=遠距離・しなやか、トト=支援・小柄で丸い、コハク=走者・細身で疾走感） |
| `assets/prototype/cats/{guard,slinger,runner}.png` | 一時増援3役（盾持ち前衛/遠距離/走者） |
| `assets/prototype/enemies/{ash_mouse,soot_weasel,sack_mole,scrap_crow,smoke_bat,spark_gecko}.png` | 通常敵6種（FLOORS_1_10_DESIGN §6） |
| `assets/prototype/enemies/{ledger_owl,blackwing_guard,kagetsubasa}.png` | エリート2+ボス（ボスは1素材+演出で3形態表現） |
| `assets/prototype/bg/{floor_ruined,floor_living,title}.png` | 荒廃した灰色の階 / 暖色の制圧階 / タイトル（不透過、2:3または1:1） |
| `assets/prototype/shops/{guild,fish_diner,claw_forge,clinic}.png` | 店舗4種の店内シーン（不透過3:2） |

未生成の画像があってもゲームが起動すること（プレースホルダのフォールバック描画を用意）。

## 3. 合格条件（全て第三者が画面録画で検証可能な形）

### 戦闘の可読性（最重要）
1. 呼び鈴を押すと100ms以内に一次反応（鈴が沈む・光る・音）があり、増援猫が**入口から戦線まで実際に走る**（戦闘幅の30〜45%を650〜1,000ms）
2. 攻撃は「予備動作→接触/弾着→ダメージ数字→HP減少→敵の反動→50〜80msヒットストップ→復帰」の順に目で追える。命中・HP減・反動は±50ms以内に同期。遠距離は弾着前にHPを減らさない
3. 猫同士・猫と敵が**重なって判別不能にならない**（レーン/前後列の縦オフセット＋個体ごとの横位置）。接敵時に視覚的に離れたまま攻撃しない
4. 敵は複数同時に存在し、前衛/盾/飛行/遠距離/後列支援の役割差がデータ構造と見た目の両方にある。単体敵のHP差し替え方式は禁止

### 画面占有率
5. 390px幅で、戦闘領域（塔の世界）が画面高の55%以上。常駐UIは下部の呼び鈴・強化・簡易HUDに限定し、モーダルは全面を覆わない

### イベントの視覚化
6. 階制圧は「勝利保持→報酬表示→猫が階段へ移動→実際に登る→次階へ入る」（1.6〜2.2秒）をアニメーションで見せる。トースト文章のみの通知は禁止
7. 制圧した階は灰色→暖色に変わり、店舗を選ぶと店内で猫が働き、物資（魚料理/包帯箱等）が上階へ運ばれる**配送アニメーション**が定期的に見える。店舗効果の発動は数字だけでなくアイコン+エフェクトで示す
8. 猫の解放（ルナ=3F配送条件、トト=5F救出、コハク=8F条件）は自動加入トーストではなく、**条件達成→救出/加入の演出**で行う。未解放猫の条件と進捗は猫一覧で常時公開

### バランス（プロトタイプ仮値でよい）
9. 1Fの最初の敵は呼び鈴2〜3回・約8〜12秒で撃破できる（開始直後の即殺なし）。5F時点の1敵撃破は約6〜10秒（DPS38 vs HP483のような長時間停滞なし）。敗北もあり得る
10. 強化（コイン消費）で次の撃破が目に見えて速くなる

### 1〜10Fの縦切り
11. FLOORS_1_10_DESIGN §11 の各階進行（1F呼び鈴習得〜10Fボス3形態撃破）が一続きで遊べる。8Fは封鎖門+複合敵の壁、9Fは遺物3択、10Fボスは3形態を瞬殺で飛ばさない

## 4. 検証

- `node --check` 相当の構文検証 + ローカル静的サーバで起動し、上記合格条件を1項目ずつ画面録画/スクショで確認
- README に起動方法とプロトタイプの位置づけ（旧v0.8.2との関係）を追記
